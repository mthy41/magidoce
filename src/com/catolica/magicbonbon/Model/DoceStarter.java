package com.catolica.magicbonbon.Model;
import com.raylib.java.textures.Texture2D;

public class DoceStarter extends Doce{
    public DoceStarter(
        Texture2D texture,
        float dropValue
    ){
        super(texture, dropValue);
    }
}
